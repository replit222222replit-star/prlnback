rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /neo_genesis_logs/{document} {
      allow read, write: if request.auth != null;
    }
    match /neo_genesis_commands/{document} {
      allow read, write: if request.auth != null;
    }
  }
}
